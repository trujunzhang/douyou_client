class AvatarUser {
  String? uniqueId;

  // user(2)
  final String? username;
  final String? avatarUrl;

  AvatarUser(
      this.uniqueId, // uniqueID
      this.username, // user name
      this.avatarUrl);
}
